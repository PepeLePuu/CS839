#include <stdio.h>
float average(float a, float b)/*a type like "float" is the type of data that the functions return each time it's called.*/
{
return (a + b) / 2;/*the body of a function must have a return to pass the argument(s) within in(eg. a, b)*/
}
